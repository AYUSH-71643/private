#include <stdio.h>
#include <math.h>

int main() {
    float r, h;
    printf("Radius is: ");
    scanf("%f", &r);
    printf("Height is: ");
    scanf("%f", &h);
    printf("The volume of the Cylinder: %f
", M_PI * r * r * h);
    return 0;
}