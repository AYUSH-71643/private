#include <stdio.h>

int main() {
    int num;
    printf("Enter any number: ");
    scanf("%d", &num);

    if (num % 1 == 0) {
        printf("%d
", num);
    } else {
        printf("%d
", num);
    }
    return 0;
}